'use client'

// Absence Summary is now part of the unified Attendance Chart page (toggle dropdown).
// This page forwards directly to that page pre-set on the Absence Summary view.
import AttendanceChartPage from '../chart/page'

export default function AttendanceSummaryPage() {
  return <AttendanceChartPage />
}
